package printscript.common.token;

public enum TokenType {
    LET,
    IDENTIFIER,
    NUMBER_TYPE,
    STRING_TYPE,
    BOOLEAN_TYPE,
    NUMBER_LITERAL,
    STRING_LITERAL,
    TRUE,
    FALSE,
    CONST,
    IF,
    ELSE,
    ASSIGN,
    COLON,
    SEMICOLON,
    PLUS,
    MINUS,
    STAR,
    SLASH,
    PRINTLN,
    LEFT_PAREN,
    RIGHT_PAREN,
    LEFT_BRACE,
    RIGHT_BRACE,
    EOF
}
