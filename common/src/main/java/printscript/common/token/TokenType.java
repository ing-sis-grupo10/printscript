package printscript.common.token;

public enum TokenType {
    LET,
    IDENTIFIER,
    NUMBER_TYPE,
    STRING_TYPE,
    NUMBER_LITERAL,
    STRING_LITERAL,
    ASSIGN,
    COLON,
    SEMICOLON,
    PLUS,
    MINUS,
    STAR,
    SLASH,
    PRINTLN,
    LEFT_PAREN,
    RIGHT_PAREN,
    EOF
}
