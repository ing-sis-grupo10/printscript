package parser.token;

public enum TokenType {
        LET, IDENTIFIER, COLON, TYPE, ASSIGN, SEMICOLON,
        PLUS, MINUS, MULTIPLY, DIVIDE,
        NUMBER_LITERAL, STRING_LITERAL,
        PRINTLN, LPAREN, RPAREN,
        EOF
}
